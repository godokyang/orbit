# Orbit 运行目录边界

`.orbit/` 默认存放当前 checkout 的运行状态、锁、原始 evidence、rules cache、handoff 和测试产物，因此除本文件外不进入 Git。

需要跨机器保留的知识应写入版本化位置：

- 稳定方案与决策：`docs/`
- 每个任务的精简耐久结论：默认 `orbit-summaries/`，或项目显式配置的版本化目录

这样可以保留可复核的结论，同时避免把绝对路径、临时凭证、大日志和高频运行状态带入版本历史。
