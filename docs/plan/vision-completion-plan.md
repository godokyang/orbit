# Orbit 当前计划

**最新进度（2026-09-26）：**用户已授权并完成 [OMP 接入回路与选模方案](orbit-omp-flow-and-model-picker-proposal-20260926.md) A–D 的源码实施。原生 OMP 18.3.2 隔离会话已实测候选池 TUI 批量保存/取消、明确受控请求自动建任务、讨论请求不建任务；短任务退出为 `paused`，未把它算作完成。任务内判断来源与问题集版本已入状态/事件；恢复路径和不确定入口由确定性回归验证，原生 Ask 跳过与 Jev 自动启动正样本不虚报。已安装 `orbit --version` 为 0.7.4，本次改动未安装。下段 0.7.3 安装与旧 M4 结论是实施前的**历史快照**，不再代表当前安装或本方案待实施状态；单一任务队列仍见[迁移总 TODO](omp-native-migration.md)。

当前状态（源码 0.7.4、本机已安装 0.7.3，2026-09-26）：0.7.4 已更新 OMP 入口最低版本门，尚未重新安装；历史 0.7.3 已落地并安装（提交 `2157f28b1b084b4bd31d23c6399021b4b7726395`，安装时工作树 clean；`sh install.sh` 成功，release `cbf09632343fc0ed2a305d8b`，`content_digest 7b9fccf24ef210ed74053ca9674e665466dbd13172b171f1361ddc549a78e049`，`installed_at 2026-09-25T17:37:00Z`，日志 `/tmp/orbit-073-install-20260926.log`）；0.7.3 确定性门禁已通过（`npm test` exit 0，日志 `/tmp/orbit-bugfix-npm-test-20260926.log`，代码修复后的完整门禁；`npm pack --dry-run --json` exit 0 且 48 文件，`/tmp/orbit-073-pack-final-20260926.json`；`check:version` 与 `git diff --check` 通过）；`orbit doctor --json`（从 `/` 执行、无 TASK）exit 0：environment／installation／omp_entry `ready=true`，`connection.ready=null`（“未验证（未提供已有会话）”），整体 `ready=false` 属 connection 未验证，非安装失败（在 Orbit 仓库内运行会因无 TASK 选中唯一任务 connection 而 exit 2 `unsupported session provider`，非环境继承）；**0.7.3 未另跑真实模型任务，不能说本版已有真实模型验收**；0.7.2 在 Zeen 实际任务 `e337c16d-c848-456c-9565-7492889b3b37` 暴露 JEV 每次 `TypeSafe assessment failed: JSON::GeneratorError`（git diff 摘录按字节预算切断 UTF-8 字符）与旧任务状态栏，该任务最终 `paused`、`stop_confirmation.confirmed=true`；0.7.3 的两处修复只经确定性回归与本机安装验证。以下 0.7.1 安装与接入验收保留为历史证据。单一 OMP 宿主的 M0–M3 已实现并通过确定性测试与本机安装切换；历史验收安装是 0.6.18（digest `06dba0f1db005f82…`，installed_at `2026-09-24T21:55:54Z`；本机当前无该安装）。**冻结 #9 已 PASS**（staged `0bc4ec93`：成员独立面→中期 fresh finding F1→`correction_sent`→同 Root 修复→终检 `resolved_ids=['F1']`→显式 stop）；**0.6.18 live JEV 费用门事实**：`subscription_quota` typed 证据请求/提交/缓存新增/三问（0.54/0.30/0.57）declined 全链留证（`249935cc`）；L1（`42d1977e`）无 hint、无成员；stale finding 经复核 resolved，无 `correction_sent`。K3 只证明无成员 finding 纠偏收敛；GLM 中文与英文同模型对照都 stage2 declined，不算成员正样本，也不认定语言是原因。J 的 member-v2 终态仍是 `failed`。**M4 整体闭合——冻结 #1–#9 全部通过**（#5 为用户批准的组合证据 PASS：同版本重复纠正确定性回归证明、纠偏收敛与过期拦截真实任务证明；`finding_repeat_ignored` 未真实触发已注明；hint 正样本为后加跟踪项；`61bbd66d` 只是显式派发的机械路径正样本；`c8d89ea6` 只证明有成员在途时的异常停止确认，不计完成）。**唯一执行队列是[单一 OMP 改版总 TODO](omp-native-migration.md)**，本文件不再维护第二份待办。历史上曾重新安装 0.6.18 并通过门禁（该安装本机现已不存在）；模型侧 typed 费用路由与三问 declined 已 live 实测（0.6.18 `249935cc`）；缺的正样本 hint 为可选跟进、非阻塞。**0.7.1 本地安装与接入验收（2026-09-25，历史）：**该版最终安装已执行 `sh install.sh`（release `d4f7194c84d401dd1c3976ec`，source commit `1cee2166`（dirty），`content_digest e0e1e336199d10d196ddd2add1ac865e8bca25064bf3b923101adb95cb1c2799`，`installed_at 2026-09-25T15:34:53Z`；完整门禁通过；重装原因是根 `README.md` 属打包文件、文案变更改变摘要）；此前的 `417a0ff4…`（release `ee7fbb9e…`，15:26:56Z）是第九个隔离任务 `final_gate` 所用构建（`complete` 并确认停止），更早的 `8ddc99b3…`（release `e666471e…`，14:06:52Z）是前八个隔离真实任务的安装；新的 `final2` 隔离任务（`61aba2a4-91db-4f70-8ac3-3d2d0cad290b`）已在该安装上结束：`complete`、`stop_confirmation.confirmed=true`、1 次 manual_check 无 findings、`finalization_notice` 与 `completed_via_finalized_stop` 落盘，p6 OMP 已退出；本轮按已证实范围收口，`delegation_hint` 正样本改为后续独立目标。本轮完成硬门、显式暂停、原生 Esc 中断与检查纠偏收尾已有真实证据，`delegation_hint` 派发正样本仍未取得（`parallel` 为 `root_without_hint`，不标通过），详见 [Orbit OMP 接入验收记录](../reference/orbit-omp-access-acceptance-20260925.md)。运行语义以[任务运行合同](../../contracts/task-runtime.md)与 [ADR-008](../adr/008-omp-native-collaboration-base.md)为准。

## 历史阶段（已结束，保留结论与链接）

- **独立任务运行与真实验收（2026-09-14）**：独立任务进程、原文与指定依据留存、固定内容快照、约定时间检查、向已有 Root 投递纠正、按需独立裁定与原生停止确认；旧运行架构已整体删除，无兼容层。见[底层真实验收](../reference/orbit-runtime-acceptance-20260914.md)。
- **日常流程交付**：Codex 原生 TUI 入口、MCP、受控执行成员、结果回收与整项停止；见[日常使用交付计划](user-experience-plan.md)与[用户流程验收](../reference/user-flow-acceptance-20260914.json)。
- **安装、更新与版本管理**：远程固定提交、失败保留旧版、按 lease 保留旧 release、skill 由 npx skills 独立管理；见[进阶使用参考](../reference/usage-reference.md)。
- **OpenCode 与 OMP 正式接入（历史路径，已由 ADR-008 取代）**：见 [OpenCode 验收](../reference/opencode-runtime-acceptance-20260914.json)、[OMP 验收](../reference/omp-runtime-acceptance-20260914.json)。
- **执行协作与检查回路调优、JEV 委派判断、用户结果补齐**：见[总调优计划](orbit-execution-review-optimization.md)、[JEV 委派判断专项计划](jev-delegation-optimization.md)、[用户结果补齐计划](user-outcome-completion-plan.md)。
- **历史多宿主接入顺序**（pi、Kimi Code、Grok、dsh、Cursor Agent）已取消；目标改为 ADR-008 的单一 OMP 路径，其他宿主不再是待办。
