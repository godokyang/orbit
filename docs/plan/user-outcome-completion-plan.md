# Orbit 用户结果补齐计划（收敛版）

状态：2026-09-18 方案稿。本文安排下一步验证与交付，不改变现行产品语义。涉及检查时机、旧结论投递、成员归属或停止范围的改变，先核对并按需更新[任务运行合同](../../contracts/task-runtime.md)与[ADR-007](../adr/007-task-runtime-refactor.md)。

## 用户结果

用户把任务交给自己惯用的 Coding Agent 后，应以合理的时间、资源和人工投入，得到符合原始意图、经过必要验证、可安全中断和接续的成果。Orbit 只有在相较 Agent 单独工作产生可观察收益时才值得增加运行机制。检查、Jev、成员和 Herdr 都是达成结果的候选手段，不以功能齐全本身作为完成条件。开发侧自行验证，不让用户承担测试。

## 当前事实与优先问题

- 当前工作树已有 Jev 三项判断和 Codex MCP `orbit.task` 改名，已按 patch 升到 0.6.2 并本地安装；启动中的旧会话仍需重开才能加载新版。TypeSafe skill 是开发指导，不是运行时插件。
- 一次真实开发任务中，7 次已完成检查全部过期，Orbit 投递纠正为 0；记录的检查用量为 4,246,313 tokens，包含缓存口径。检查者曾提出有用问题，Root 是自行查询任务记录取得，不能把这次经历算作自动纠偏成功。详见[独立审查及复核](../reference/project-review-20260918.md)。
- 现有受控遗漏验收证明最小纠偏与停止路径可用，但还不能证明复杂日常任务普遍节省成本或减少人工介入。真实任务没有执行成员，不能由此推断跨 Agent 协作没有需求；用户明确提出 Herdr 协作缺口。
- 现有 `delegate` 创建同宿主成员；Herdr 外部成员尚未自动纳入 Orbit 的结果回收和停止范围。可用 kind 必须在实际环境中核实，不能从支持列表推断。

**下一步先处理检查回路的有效性和成本。** Jev 真实效果、跨 Agent 协作分别验证，不把它们绑成一次平台工程。

## 防止扩张的规则

1. 一次交付一个用户可见结果；前一段不因后续构想而保持未完成。
2. 先用已有 Root、检查者、任务记录及宿主原生接口。若一段需要多个新适配器、界面解析、双套兼容路径，或新增代码明显超过直接用户行为所需，先复审方向。
3. 记录检查是否过期、问题是否被 Root 接受、实际用量及未知部分；不设凭一次样本得出的固定节省率、纠正条数或接受率门槛。没有问题的任务不应为了达标制造纠正。
4. 旧版本检查只作为历史记录；当前合同要求重新核对变化，不能把旧 finding 直接当作当前问题投递。若要改变这一点，先明确允许的语义和风险，再修改合同与实现。
5. 少量测试覆盖关键行为，真实 Agent 和真实 Jev 验证分别说明证明范围；不得以夹具通过代替日常效果。发布、安装、推送分别记录实际结果。

## 切片 A：让检查结果有效到达 Root，并减少无效检查

- [x] 用已有任务记录复盘检查启动原因、检查者返回的 `next_check_seconds`、执行与文件变化时间、过期原因和用量。30–60 秒重启间隔是已观察到的重复触发因素；具体调度改动以此为依据。
- [x] 在保持约定时间、Root 交付检查及版本核对语义的前提下，收敛持续编辑时的重复完整检查。过期结论不直接纠正当前版本；让需要纠正的问题在新版本核对后自动送达 Root，不依赖 Root 轮询状态。
- [x] 在现有 `orbit status` 清楚显示最近检查是否过期、有无待处理问题及下次检查依据。不建新的事件总线或费用平台。

完成条件：开发侧用一条有实际问题的受控路径核对自动投递，再用代表性的持续编辑任务观察检查次数、过期原因、用量和用户可见结果。若真实任务没有问题，不要求出现纠正消息。修正仍导致大量重复过期或明显推迟重要纠偏时，停止增加调度层，重新检查检查范围和耗时。**本段独立交付，不等待 Jev 或 Herdr。**

## 切片 B：验证已写 Jev 接入的实际增益

- [x] 核对 Jev 输入保留当前有效要求。当前只取最近 20 条修改，不能仅凭这个上限宣称所有有效要求已覆盖；超出预算时明确标出省略范围，不把缺失依据的判断当问题证据。
- [x] 在最终源码上验证现有回归与普通宿主的真实 Jev 判断；观察它触发的过程检查是否给出有用发现，以及是否减少无效完整检查。服务失败应沿用既有检查路径。
- [x] 根据结果收束实现和发布准备。用户已决定以环境中的 `TYPESAFE_API_KEY` 全局启用、项目可关闭；若证据显示默认启用会明显损害效果或成本，报告事实并先修正或重新讨论该决定，不把审查建议自行改成新产品规则。（已按 patch 升到 0.6.2 并本地安装；未推送、未发布）

完成条件：新启动的日常任务能使用已配置的 key、留下可核对的 Jev 判断，且能说明新增判断的实际收益与额外消耗；`orbit.task` 可调用，安装版本可由 `orbit version --json` 核对。旧会话不冒充已更新。

## 切片 C：让 Herdr 中获授权的 Agent 能协作并有限停止

用户要的是 Orbit 能调用实际可用且获授权的不同 Coding Agent，而不是固定调用 OpenCode。先用一种 kind 验证最小接线，是为了找出共同的启动、回收和停止机制；交付还须验证至少另一种获授权且实际可用的 kind。当前任务没有使用同宿主 `delegate`，不能作为删除跨 Agent 需求的理由。

- [x] 用第一种非 Codex kind 核对启动、真实工作、结果读取和停止机制。（Herdr OpenCode：启动、真实工作、文件化结果读取可用；`esc` 未停止，`ctrl+c` 结束成员及前台进程，脱管后台服务在 pane 关闭后仍存活，见[验证记录](../reference/check-loop-acceptance-20260918.md)）
- [ ] 按下述有限停止边界，用不启动脱管服务的真实子任务复核第一种 kind：Root 能收到并集成结果，用户中断后确认该登记成员及其受控执行退出；保留原失败记录，不把新边界伪称为当时已验证。
- [ ] 接入最小成员路径：Root 从 Herdr 实时发现的可用 kind 和用户授权范围内选成员，Orbit 登记成员身份、投递要求、回收结果并确认有限停止；保持已有同宿主 `delegate` 路径。若必须持久表达授权范围，只增加必要的少量配置，不把 OpenCode 名称写死为唯一可用 kind。
- [ ] 用至少另一种获授权且本机实际可用的 kind 跑同一条真实子任务交付与有限停止路径；该 kind 若缺少必要控制接口，就记录其具体缺口并保持不可用，不以 OpenCode 的通过代替它的验收，也不为此扩成通用编排平台。

有限停止边界：Orbit 只承诺停止本任务登记的成员 Agent、其当前受控执行和宿主能识别的附属前台工作；若通过明确接口登记了后台工作，也要核对其退出。未经登记、脱离宿主管理的后台进程不属于可控范围；若已知仍有与任务相关的脱管进程，必须报告残留，不能宣称整项任务已安全停止。先复用 Herdr 现有中断能力并核对实际进程状态，不增加全机进程扫描或通用进程管理器。

停止条件：某个 kind 无法可靠回收结果或确认上述范围内的执行退出时，记录接口缺口并停止接入该 kind；不转向屏幕解析、自写终端按键协议或盲目接入更多 kind 掩盖失败。Herdr 自身提供的 `send-keys` 只有在实际退出得到确认时才可作为中断手段。完成条件是 Root 在真实任务中能按授权选择至少两种已验证的 kind，各自收到并集成明确子任务结果，用户中断后各自受控工作实际退出。只声明逐种验证过的支持范围。

## 切片 D：协作路径成立后评估 Jev 委派提示

只有切片 C 证明可用成员、结果回收和有限停止后，才评估增加“当前是否可能有适合独立交付的子任务”这一窄判断。Jev 只提醒，Root 决定是否派发和选用哪个已验证、获授权的 kind。比较有、无提示时的实际分工收益与调用成本；没有可观察收益就停在 Root 手动分工，不堆阈值或路由规则。

后续增加到第三种及更多 Agent kind、通用注册中心、完整跨供应商费用统计和自动 Root 替换都需要新的实际需求及原生能力证据，不属于上述切片的默认完成条件。

## 给接手实现的 Agent

- 当前仓库有未提交的 Jev、MCP 改名、测试和文档修改。先读根 `AGENTS.md`、[开发流程](../agents/development-workflow.md)、[交接](handoff.md)、本计划及相关合同，再看 `git status` 与实际 diff；保留已有工作，不重置或覆盖。本机已安装 0.6.2，但启动中的旧宿主会话不会自动加载新版；以 `orbit version --json` 和新会话实际行为核对。
- 按 A → B → C → D 顺序交付，每段独立核对完成条件和真实证据。某段遇到明确停止条件时记录已完成、未完成及原因，不靠增加框架绕过，也不把依赖它的后续切片标成完成。实现期间只维护一份必要的总进度，不再复制计划文档。
- 在同一 pane 中使用 OpenCode 开发 Orbit，只说明实现 Agent 换成了 OpenCode；这不证明 Orbit 已能启动、回收或停止 Herdr 中的其他 Agent。切片 C 对每种纳入支持的 kind 分别用实际成员验证结果与有限停止；不能用当前开发 pane 充当新成员。
- 若实现选择会改变旧结论处理、定时检查、成员归属或停止语义，先改对应合同和 ADR，再改代码；不能为通过测试而降低原有保证。若只有少量明确用户行为需要验证，复用已有测试，不建新的测试体系。
- 每段回报源码改动、实际运行路径、检查用量和未知部分；分别说明测试、真实 Agent 验证、本机安装、提交与推送的状态。安装或切换前核对正在运行的任务与宿主会话；实现本文不自动授权推送或对外发布。

## 实施状态（2026-09-18）

- 切片 A：已实现并验证。真实受控路径证明过期问题经新版本核对后自动投递，持续编辑任务观察到约定间隔下限、分项过期原因与自动纠正；完整测试 17 项通过，0.6.2 新宿主会话复核 7 次检查/2 条自动纠正/最终完成。证据与边界见[检查回路与 Jev 实际验证](../reference/check-loop-acceptance-20260918.md)。
- 切片 B：输入省略范围、真实 Jev 判断与用量记录已核对；本次样本未观察到过程检查增益或完整检查减少，额外消耗已量化。已按 patch 规则升到 0.6.2 并从工作树本地安装，`orbit version --json` 与交接一致。
- 切片 C：首次 OpenCode 实验核对了启动、真实工作与文件化结果读取；`esc` 未停止，`ctrl+c` 结束成员和前台进程，pane 关闭后脱管服务仍存活，故按当时较宽的停止条件结束，未接入适配器。用户随后选择有限停止并要求覆盖不同 Agent kind；上述复核、最小接线和第二种 kind 的真实验收均未完成。切片 D 因依赖未满足未开始。
- 运行中安装曾导致本仓验证任务 `30586614` 读取旧 release 规则库失败（`failed`，停止已确认）；本轮不扩展安装器热更新，按“结束任务→安装→重开宿主会话”顺序使用，限制记录在[当前限制](debt-ledger.md)。
