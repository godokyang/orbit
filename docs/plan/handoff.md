# Orbit 当前交接

2026-09-14：独立任务运行重构及两条最小真实验收已完成，随后清理过时文档、补齐安装更新与版本管理，并修正 skill 主动调用说明及独立项目定位；日常会话接入仍需单独完成。

## 从这里继续

先读根 [AGENTS.md](../../AGENTS.md)、[开发流程](../agents/development-workflow.md) 和 [当前计划](vision-completion-plan.md)。涉及设计再读 [ADR-007](../adr/007-task-runtime-refactor.md) 与 [运行合同](../../contracts/task-runtime.md)，不要重新执行已结束的旧阶段。

## 已完成

- 新入口 `scripts/orbit` 使用任务运行模块，处理原文、快照、现有主执行 Agent（Root）连接、独立检查、纠正裁定和实际停止。旧协议、命令、模板及相关测试已退役，不读取旧数据。
- 真实遗漏纠正与真实停止均通过，10 个确定性行为测试、安装／卸载、skill 校验和打包检查通过。范围与原始结果在 [验收记录](../reference/orbit-runtime-acceptance-20260914.md)。
- 旧 ADR、阶段工单、重复交接和讨论材料已从当前目录删除。有效取舍归入 ADR-007，事故教训归入 [工程经验](../reference/engineering-lessons.md)；历史按需从 Git 查阅。
- 验收自有进程、tab 和临时目录已清理，两个夹具会话归档。外部参考资料仍在本地保留并被 Git 忽略。

## 安装与版本管理

- 产品版本统一读取 `package.json`；`orbit --version` 查询版本，`orbit version --json` 查询安装来源与内容摘要。
- 新版先准备并校验，再同时切换 CLI 与 skill；更新失败保留旧版，成功后按清单清理淘汰文件。卸载保留用户额外文件和空目录。
- 新增四条安装行为测试及独立核查通过，操作见 [README](../../README.md)，范围见 [验收记录](../reference/orbit-runtime-acceptance-20260914.md)。维护需先结束 Orbit 任务；旧安装格式不迁移，没有热更新或自动发布。
- 用户要求的 upstream 子模块已移除。临时验证资源已清理，本机全局安装保持原样；提交及远端同步状态以 Git 为准。

## Skill 入口与独立使用说明

- 简介直接写明已授权复杂执行任务应主动使用，无需用户点名；讨论、小修和同一任务的重复调用有明确边界。Codex 元数据允许隐式调用。
- 主要入口定义 Root 为当前负责整项任务的 Agent；公开文档移除特定外部项目描述，本地参考资料及其忽略规则保留。
- Skill 格式、隐式调用配置、包内文件、Markdown 链接与 CLI 帮助检查通过。尚未在新会话中验证自然请求能稳定触发；未更新全局安装，提交及远端同步状态以 Git 为准。

## 日常使用尚缺的一步

2026-09-14 维护者本机核查：PATH 中的 `orbit` 仍为 0.1.15，默认 Codex 控制 socket 不存在。该记录只是当时环境快照，后续应重新核对，不能据此推定所有机器的状态。

因此，专用 app-server 上的验收通过不等于当前普通终端会话已经可以直接接入。下一步应优先解决实际使用环境中的新版入口与原生连接，并保持已有 Root；具体缺口在 [当前限制](debt-ledger.md)。本轮实现了安装管理，没有切换全局安装、启动服务或接管会话。

用户此前授权的重构提交与推送已完成。后续提交及远端同步状态以 Git 为准；不自动发布 npm 包或修改外部项目。
