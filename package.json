{
  "name": "@godokyang/orbit",
  "version": "0.6.6",
  "description": "Independent task execution checks for existing coding sessions.",
  "bin": {
    "orbit": "scripts/orbit"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "README.md",
    "install.sh",
    "uninstall.sh",
    "npm-shrinkwrap.json",
    "lib",
    "skills/orbit",
    "scripts/orbit",
    "scripts/codex-socket.cjs",
    "scripts/orbit-mcp.cjs",
    "contracts/task-runtime.md",
    "contracts/check-result.schema.json",
    "scripts/manage-install.rb",
    "scripts/check-version.rb",
    "plugins"
  ],
  "scripts": {
    "test": "npm run check:version && sh -n install.sh && sh -n uninstall.sh && ruby --disable-gems tests/workspace_snapshot_test.rb && ruby --disable-gems tests/task_runtime_test.rb && ruby --disable-gems tests/codex_connection_test.rb && node tests/mcp_test.cjs && node tests/opencode_test.mjs && node tests/omp_test.mjs && ruby --disable-gems tests/cli_test.rb && ruby --disable-gems tests/install_test.rb",
    "check:version": "ruby --disable-gems scripts/check-version.rb"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "ws": "8.21.3",
    "zod": "4.6.5"
  }
}
