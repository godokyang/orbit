# AGENTS.md — 工程协作与测试规范

> 本文件约束 Codex 在本仓库中的工程行为。核心目标：**正确、高质量、可维护地完成用户任务**，防止"为测试而测试"导致任务跑偏。
>
> 本文件是**支持读取该文件的开发 Agent 的客户端纪律**，不是 Orbit 产品 authority：它不产生任何 gate/evidence/state 事实，不影响 v2 合同；产品 authority 是 active ProjectPolicyRevision / TaskRevision 与受控 writer/validator，两者冲突时以产品 authority 为准。

## 按需加载（动手前先读）

本文只管测试与范围纪律。产品裁决不在这里，**不要凭本文推断该做什么**。

- 在本仓做任何实现、重构或文档改动前，先读 [docs/README.md](docs/README.md)：它说明哪类事实归哪个文件，以及哪些层没有裁定权。
- 承接任何交付任务前，先读 [docs/plan/vision-completion-plan.md](docs/plan/vision-completion-plan.md) 的「如果你刚接手」表——D1–D10 是全部有约束力的裁决，含已被否决的备选。
- 动 `lib/` 或 `contracts/` 前，查 [docs/plan/debt-ledger.md](docs/plan/debt-ledger.md)：有意推迟的项目及其解除条件都在那里。
- 语义合同以 `contracts/orbit-v2/` 与 `docs/adr/` 为准。散文与合同冲突时以合同为准。

**禁止只在对话中拍板。** 任何架构或产品结论必须同步写进对应权威文件并记录理由；只存在于对话里的裁决，下一个上下文就没有了。

## 任务优先级

1. 理解真实需求
2. 完成核心功能
3. 保持修改范围可控
4. 验证关键行为
5. 补充必要测试

不要为了追求完整性或覆盖率而扩大任务范围。

## 测试定位

测试是辅助验证手段，不是目标本身。它用于：

- 验证业务正确性
- 防止核心功能回归
- 提供修改信心

## 编写测试前

新增测试前必须回答：

1. 这个修改影响什么用户行为？
2. 哪些业务规则必须保证？
3. 哪些场景存在真实回归风险？

只有能提供实际价值的场景才写测试。

## 测试范围

**优先测试：**

- 核心成功流程：正常创建、更新、提交、状态流转
- 重要业务分支：权限不足、状态不允许操作、关键参数校验失败
- 历史问题与高风险逻辑：曾出 bug 的场景、容易被改坏的规则

**避免测试：**

- 框架自身行为：Spring/MyBatis 已保证的功能、第三方 SDK 内部逻辑
- 无业务价值的边界：极端字符串长度、不可能出现的数据、人工无法触发的异常
- 实现细节：私有方法步骤、getter/setter、临时变量、重构不影响的结构
- 假设性风险：仅"理论上可能发生"，而非真实影响业务

## 防止测试爆炸（硬限制）

- 新增测试数量 ≤ 10 个
- 新增测试代码 ≤ 300 行

超出限制必须先行说明：为什么需要更多、覆盖哪些业务风险、为什么已有测试不足，得到确认后才能扩大。

## 实施顺序

理解需求 → 拆解核心流程 → 实现主要功能 → 运行已有测试 → 补充必要测试 → 验证需求完成

禁止：

- 因测试覆盖不完整而暂停主功能开发
- 在核心功能未完成前大量编写测试
- 为测试而改变业务实现

## 覆盖率原则

覆盖率不是最终目标。不要因为覆盖率下降或分支未覆盖而机械加测试。

判断标准：删除这个测试后，团队发现真实问题的能力会下降吗？不会 → 不保留。

## 测试设计方式

用 Given / When / Then 描述用户行为：

- Given 一个待支付订单 / When 用户取消订单 / Then 订单状态变为取消

不推荐：null 组合、随机字符串、不可能状态、内部方法调用路径。

## 任务范围控制

完成当前任务即可，不主动：

- 重构无关代码
- 修改无关模块
- 引入新测试框架
- 优化已有架构
- 清理历史代码

除非用户明确要求。

## 用户要求增加测试时

不要立即生成大量测试，先输出：

1. 测试策略
2. 测试场景列表
3. 预计测试数量
4. 覆盖的业务风险

规模明显扩大时，先等待确认。

## 代码审查标准

新增测试前问自己：一个有经验的工程师是否愿意长期维护这个测试？答案是否定 → 不要添加。

## 需求与测试冲突时

优先完成需求主线；测试只能作为辅助验证手段，不能阻塞核心功能实现。

> 遇到"为了覆盖用例连主线任务都不管"的情况，本质是优先级排序错误，不是测试能力不足。
