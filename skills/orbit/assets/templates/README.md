# Orbit 模板目录（v1 已停用）

本目录所有 `*.v1-deprecated` 模板是 Orbit v1 schema（task.yaml、evidence.json、
roles.yaml、instances.yaml、loop-state.yaml、各类 report），**随 v1 runtime 于
2026-08-17 一并停用**：对应的 v1 命令已删除，这些模板不再有任何命令消费。

v2 的输入文件（task 定义 YAML、evidence proposal、gate evaluation 定义、
finding resolution 定义）不使用这些模板——格式见 `orbit v2 --help` 与
`skills/orbit/SKILL.md` 的示例。v2 专用模板待 v2 运行时文档重写时提供
（ADR-005 修订记录第 7 条，未完成项）。
