# AGENTS.md

> 可选项目索引示例，不是使用 Orbit 的前置条件，不由 Orbit 自动安装。将下文 `rules/` 替换成项目实际可读取的规则路径；项目已有规范优先按当前授权处理。

本文件是**这个用户项目**的路由器，只放项目入口和按需规则路径。

专项判据在 `rules/tasks/*.md`。共用职责、停下与交接只看 `rules/shared/escalation-payload.md`。链接不等于加载；不要把规则正文抄进本文件。

把下面标成 `<PLACEHOLDER>` 的行换成这个项目自己的事实。
不要填成别的仓库的目录、命令或产品定义。

## 这个项目是做什么的

`<PROJECT_PURPOSE>`

一到两句：给谁用、解决什么问题。不要写实现清单。

## 这个项目明确不做什么

`<PROJECT_NOT>`

列出本仓库拒绝承担的范围。没有就写「尚未声明」，不要编。

## 命令

把标记换成这个项目真实会跑的命令。没有就保持标记，不要抄别处的。

- 安装：`<INSTALL_COMMAND>`
- 测试：`<TEST_COMMAND>`
- 检查 / lint：`<CHECK_COMMAND>`

## 目录（可选）

需要时补这个项目自己的入口，不要留空目录树当装饰：

- 源码：`<SRC_DIR>`
- 测试：`<TEST_DIR>`
- 文档：`<DOC_DIR>`

## 按需规则

做下列动作前读取对应文件。表里只有触发和路径，不要在本文件复写判据。

- 实现功能、重构、按当前任务改代码 → `rules/tasks/minimal-implementation.md`
- 复现、隔离或修复一个失败症状 → `rules/tasks/targeted-fix.md`
- 新增、修改、删除或为测试辩护 → `rules/tasks/test-selection.md`
- 独立判断原始指令是否被满足，同时找漏做／做错／做多 → `rules/tasks/review.md`
- 新增或重命名用户可见符号、文件、API、对外陈述 → `rules/tasks/vantage-audit.md`
- 改 parser / resolver / validator / 状态机 / artifact writer / gate / tool 边界 → `rules/tasks/structured-boundary.md`
- 改 CLI、安装、卸载、迁移、打包、release 资产 → `rules/tasks/mutating-surface.md`
- 从原始指令整理成功标准 → `rules/tasks/quality-outcome.md`

项目参考资料中的示例不自动成为执行规则。
